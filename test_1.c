#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

//int main()
//{
// 
    //float a = 0, b = 0;
    //float sum = 0;
    //printf("请输入两个任意位数的数字，两个数字中间要有空格\n");
    //scanf("%f %f", &a, &b);
    //sum = a + b;
    //printf("和为 = %f", sum);
    //
    //printf("\a");//唤醒电脑的蜂鸣，会叮咚叮咚两声
    //
    //int a=0, b=0;
    //scanf("%d%d", &a, &b);
    //printf("%d",a + b);

    //double a = 9 / 2.0;//除法要想得到小数必须分号两边至少有一个小数
    //printf("%f", a);
    
    //int a =0;
    //printf("%d", ~a);//波浪号表示按位取反

    //int a = 1;
    //int  b = (++a) + (++a) + (++a) + a;      //左边两次结果说明无论是先给a+1后使用还是先使用后给a+1，
    //printf("%d，%d", a, b);//输出结果为4,16  //都是先把整个一行的程序中的+1加完再使用或者先使用完再+1。

    //int a = 1;                               //（但是这样写代码是错误的方式，没有人会这样写代码，
    //int  b = (a++) + (a++) + (a++) + a;      //而且左边这个在不同编译器上运行结果可能不同，因此这个代码不值得研究！）
    //printf("%d，%d", a, b);//输出结果为4,4     

    //int a = 0, b = 3, c = 5, d = (a = b + 2, c = a - 4, b = c + 2);//逗号表达式，d的值为最后一个逗号后面等号的值
    //printf("%d %d %d %d", a, b, c, d);

    //unsigned int a = 100;
    //typedef unsigned int u_int;
    //u_int b = 10;
    //printf("%d %d",a, b);
//    int a = 0, b = 0;
//    scanf("%d %d", &a,& b);
//    int max = Max(a, b);
//    printf("%d", max);
//
//     return 0;
//}
// 
//int Max(int x, int y)
//{
//    if (x > y)
//        return x;
//    else
//        return y;
//}
//int main()
//{
//    int M = 0;
//    scanf("%d", &M);
//    if (M % 5 == 0)
//        printf("YES");
//    else
//        printf("NO");
//    return 0;
//}
//int main()
//{
//    printf("     **\n     **\n************\n************\n    *  *\n    *  *");
//    return 0;
//}
}
